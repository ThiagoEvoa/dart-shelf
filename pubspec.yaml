name: backend_shelf
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  args: ^2.3.1
  http: ^0.13.5
  shelf: ^1.4.0
  shelf_router: ^1.1.3

dev_dependencies:
  build_runner: ^2.3.2
  lints: ^2.0.1
  test: ^1.22.0
